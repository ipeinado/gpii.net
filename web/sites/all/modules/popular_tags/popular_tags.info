name = Popular Tags
description = Allows the users to select taxonomy terms/tags by just clicking on them rather than typing them (auto-complete) or selecting them from a select box/checkbox/radio etc. It shows them a list of tag links, sorted by popularity (defined as how many nodes are tagged with it), up to a configurable maximum number.
core = 7.x
package = Taxonomy

dependencies[] = taxonomy

; Information added by Drupal.org packaging script on 2017-05-14
version = "7.x-1.2"
core = "7.x"
project = "popular_tags"
datestamp = "1494727091"

patch[] = Customized popular_tags.widget.inc to include the taxonomy description and frequency count in the title attribute. Widget and .module file include additional customizations from the 7.x-1.1 version to keep it working.
